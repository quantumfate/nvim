# CLAUDE.md

Read [AGENTS.md](AGENTS.md). It is the agent entry point for this repo: the
read order through the layer diagram (Readme.md), the per-directory READMEs,
the toolchain registry rule, and the gate (`just check`).

Nothing in this file overrides AGENTS.md.
